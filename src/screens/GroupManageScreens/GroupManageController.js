import React from "react";

import GroupManageView from "./GroupManageView";

export default GroupManageController = ({ navigation, route }) => {
  // console.log(route);
  return <GroupManageView navigation={navigation} route={route} />;
};
